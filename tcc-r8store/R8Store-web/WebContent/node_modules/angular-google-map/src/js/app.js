(function() {
	'use strict';

	angular
		.module('demo', ['angular-google-map'])
		.config(config)
		.run(run);

	/** @ngInject */
	function config($provide) {

	}

	/** @ngInject */
	function run($rootScope) {

	}

})();
